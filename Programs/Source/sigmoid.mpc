"""sigmoid-based OPPE func
"""
from Compiler.ml import approx_sigmoid
from Compiler.types import *

# Test performance.
N = 50
x = Array.create_from([sfix(i) for i in range(N)])

for i in range(N):
    z = approx_sigmoid(x[i])
