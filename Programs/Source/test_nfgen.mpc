import test_nfgen as tn 
program.use_trunc_pr = True 
program.use_edabit
program.use_split(3)

N = 10
sfix.set_precision(48, 96)
x = Array.create_from([sfix(i) for i in range(N)])
for i in range(N):
    y = tn.sigmoid(x[i])
